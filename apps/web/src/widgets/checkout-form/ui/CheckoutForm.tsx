'use client';

/**
 * @deprecated Use CheckoutPage (views/checkout) instead.
 * CheckoutPage manages the full multi-step checkout flow with address + payment.
 */
export function CheckoutForm() {
  return null;
}
